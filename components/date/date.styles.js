import { StyleSheet } from "react-native";
import { SHADOWS ,FONT,SIZES, COLORS} from "../../constants/themes";


const styles = StyleSheet.create({
   dates: {
      textAlign: "center",
    fontSize: 18,
    fontWeight: 'bold',
     color: '#46270a',
      fontFamily: FONT.medium,
      
    
   },

   cont: {
      paddingTop:10,
      marginHorizontal: 80,
      
      
   }
})

export default styles